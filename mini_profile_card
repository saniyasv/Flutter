import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override

  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Column(
            children: [
              Text("Top"),
              Text("Middle"),
              Text("Bottom"),
              Row(
                mainAxisAlignment: MainAxisAlignment.spaceAround,
                children: [
                  Text("Left"),
                  Text("Center"),
                  Text("Right"),
                ],
              ),
              Container(
                width: 150,
                height: 150,
                decoration: BoxDecoration(
                  color: Colors.blue, // Sets the background color to blue
                ),
                child: const Center(
                  child: Text(
                    "Hello",
                    style: TextStyle(color: Colors.white), // Makes text readable on blue
                  ),
                ),
              ),
              Padding(
  padding: const EdgeInsets.all(16.0), // Adds a nice border of space around the card
  child: Row(
    children: [
      // 1. Profile Picture Placeholder
      Container(
        width: 60,
        height: 60,
        decoration: BoxDecoration(
          color: Colors.grey[400], // Grey color to mimic a picture box
          borderRadius: BorderRadius.circular(8), // Optional: rounds the corners slightly
        ),
      ),
      
      const SizedBox(width: 16), // Adds a clean 16-pixel gap between the box and the text
      
      // 2. Text Section
      Column(
        crossAxisAlignment: CrossAxisAlignment.start, // Aligns text to the left
        mainAxisSize: MainAxisSize.min,               // Keeps column compact
        children: const [
          Text(
            "Saniya Susan Varkey", // The name to displayq
            style: TextStyle(
              fontSize: 18,
              fontWeight: FontWeight.bold, // Makes the name bold
            ),
          ),
          SizedBox(height: 4), // Tiny gap between name and role
          Text(
            "Flutter Developer / Learner",
            style: TextStyle(
              color: Colors.grey, // Subtle grey color for the subtitle
            ),
          ),
        ],
      ),
    ],
  ),
)
            ],
          ),
        ),
      ),
    );
  }
}

  
    
